export default function HookGeneratorPage() {
    return (
      <div>
        <h1 className="text-3xl font-bold">AI Tweet Hook Generator</h1>
        <p className="text-muted-foreground mt-2">
          Enter a topic and we'll generate viral tweet hooks for you.
        </p>
        {/* The form for the tool will go here in the next step */}
      </div>
    );
  }